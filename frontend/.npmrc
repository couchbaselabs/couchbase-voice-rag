; Hoist rolldown's native bindings so vitest's bundled rolldown can require()
; them via plain Node module resolution (pnpm's strict isolation otherwise
; hides @rolldown/binding-* from rolldown's wrapper at runtime).
public-hoist-pattern[]=@rolldown/*
